---
import LogoCAD from "../logo-CAD.astro"
import castell_img from '../../images/castell-pro.png'
import react from "@astrojs/react";
import Swiper from "../MySwiper.jsx"


// const  themeColor  = '#676767';
const zoomResolution = '1'
const isDark = false
const themeColor = isDark ? '#ffffff' : '#676767'


---
<script is:inline>import { transitionEnabledOnThisPage } from "astro:transitions/client";

    const element = document.documentElement;
    element.classList.toggle("dark");
    isDark = element.classList.contains("dark");
    // localStorage.setItem("theme", isDark ? "dark" : "light");     
    //    alert(isDark)   ;
    // Asigna un valor apropiado a themeColor
    // const themeColor = isDark ? 'white' : 'black';
    // alert(themeColor)
</script>

<style>
    button{
        /* background: #0062a7; */
        transition: all ease 0.3s
    }
    body {
  font-family: 'Roboto Mono Variable', sans-serif;
}
header div p{
    font-size: 1rem ;
}
    
</style>


<header id="header-hero" class="flex flex-col md:flex-row  md:flex-row p-20 gap-5 text-left text-center md:text-left rounderd-xl ">
    <div class="flex-1">
        <span class="text-black bg-third px-3  rounded-sm mr-2 mb-2 ">Full Stack Developer</span>
        <span class="text-black bg-third px-3  rounded-sm mr-2 mb-2 ">Systems Engineer</span> <br>
        <span  class="text-black bg-third px-3  rounded-sm mr-2 " style="margin-top: 20px !important;" >English speaker</span>
        <h1 class=" mt-8 text-3xl font-normal  pb-2 mb-6">Castell's Nexus: Logic and Creativity Unleashing Success</h1>
        <p class="my-4 text-xl text-body">I know you're looking for something really good to help you realize your ideas.
            </p>
            <p class="my-4 text-xl text-body mb-20">
                I'm going to give you that, Ivan Esteban Castillo.
            </p>

           <a href="https://wa.link/wrh5ev" class="text-third text-2xl hover:underline">Let's Chat</a>
       
    </div>


    <div  class="flex-1" style="align-self: center; display: flex; flex-direction: column;" data-zoom={zoomResolution}> 
      
<script is:inline>
    function toggleDarkMode() {
        const elementT = document.documentElement;
        const isDarkMode = elementT.classList.contains("dark");
        
        // Asegúrate de realizar las acciones necesarias según el valor de isDarkMode
        const themeColor = isDarkMode ? '#ffffff' : '#676767';        
          // Actualiza el tema del LogoCAD
          const logoCAD = document.querySelector('LogoCAD');
        if (logoCAD) {
            logoCAD.setAttribute('theme', themeColor);
        }
        // alert(isDarkMode+themeColor);
    }
    
    // Ejecutar cuando se carga la página
    document.addEventListener('DOMContentLoaded', toggleDarkMode);

    // Ejecutar cuando cambia el tema
    document.addEventListener('theme-change', toggleDarkMode);
</script>
        <img src={castell_img.src} width="500" height="500" alt="castell" />
       
         <button id="button-action-hero " style="max-width: 155px ; color: themeColor; border-top: 1px solid #67676700  " class="  mx-auto  px-10 py-2 bg-transparent hover:bg-sky-400 rounded-sm text-center  " >
            Click here!
        </button >
             
    </div>

    <!-- <div class="flex-1 text-right">
        <h1 class="text-5xl font-bold pb-2">Quickly Adds</h1>
        <p class="my-4 text-2xl">With cutting-edge Motion Graphics, your product stands out and sells in just 5 seconds.</p>
       
    </div>
     -->
    
        
</header>
<header id="header-hero" class="flex flex-col md:flex-row  md:flex-row p-20 gap-5 text-left text-center md:text-left rounderd-xl ">



    <div  class="flex-1 text-center" style="align-self: center; display: flex; flex-direction: column; background: red;" data-zoom={zoomResolution}> 
      
    


         
        <!-- <Swiper ></Swiper> -->
         <!-- <LogoCAD zoom={zoomResolution} theme={themeColor} theme_text="#00aeff"></LogoCAD> -->
     
             
    </div>

    <!-- <div class="flex-1 text-right">
        <h1 class="text-5xl font-bold pb-2">Quickly Adds</h1>
        <p class="my-4 text-2xl">With cutting-edge Motion Graphics, your product stands out and sells in just 5 seconds.</p>
       
    </div>
     -->
    
        
</header>
